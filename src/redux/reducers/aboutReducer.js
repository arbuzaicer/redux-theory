/*Variables section*/

const initialState = {
    title: "Hello from About Reducer",
};

const aboutReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};


export default aboutReducer ;